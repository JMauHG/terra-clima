# Terra Clima Installation

## Setup

Make sure to install dependencies:

```bash
npm install
```

Generate the Prisma Client:

```bash
npx prisma generate
```

Add your environment variables

```bash
cp .env.example .env
```

Run the server

```bash
npm run dev
```
